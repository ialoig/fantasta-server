/*
const leagueCreate = ( req, leagueName, resp ) =>
{
    let io = req.app.get('io');
    io.emit( leagueName + ".league.complete", leagueName, resp );
}

const addAttendee = ( req, leagueName, resp ) =>
{
    let io = req.app.get('io');
    io.emit( leagueName + ".attendee.added", leagueName, resp );
}

export {
    leagueCreate,
    addAttendee
}
*/